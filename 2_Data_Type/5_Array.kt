// Array is the collection of elements of the same type or its subtype of datatype, which is stored in contiguous memory.

fun array (){
    val arrays = arrayOf(1, 2, 3, 4, 5)
    for (i in arrays){
        println(i)
    }
}

fun main(args: Array<String>) {
    array()
}